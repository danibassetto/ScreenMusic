@model IEnumerable<ScreenMusic.Arguments.OutputArtist>
@{
    ViewData["Title"] = "Artistas cadastrados";
}

<div class="mt-4 mb-4 d-flex justify-content-between">
    <h4>Artistas cadastrados</h4>
    <nav aria-label="Page navigation">
        <ul class="pagination">
            @for (int i = 1; i <= ViewBag.TotalPage; i++)
            {
                <li class="page-item @(ViewBag.CurrentPage == i ? "active" : "")">
                    <a class="page-link" asp-action="Index" asp-route-pageNumber="@i">@i</a>
                </li>
            }
        </ul>
    </nav>
    <a href="@Url.Action("Create", "Artist")" class="btn btn-primary btn-lg">Criar Artista</a>
</div>

<div class="row">
    @foreach (var artist in Model)
    {
        <div class="col-sm-6 col-md-4 col-lg-3 mb-4">
            @await Html.PartialAsync("_CardArtist", artist)
        </div>
    }
</div>